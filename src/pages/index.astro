---
import { getConfig } from '../utils/getConfig.js';
const config = getConfig();
const first = config.tabs[0];
---
<html>
  <head>
    <meta http-equiv="refresh" content={`0; url=${first.id}/`} />
    <title>Redirecting…</title>
  </head>
  <body>
    <p>Redirecting to <a href={`./${first.id}/`}>{first.label}</a>…</p>
  </body>
</html> 